import React from 'react';

const ContainerWarp = ({ children }) => {
    return (
        <section className="content-warp-product">
            {children}
        </section>
    );
}

export default ContainerWarp;
